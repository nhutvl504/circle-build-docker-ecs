machine:
  services:
    - docker
dependencies:
  post:
    - docker build -t  647433451523.dkr.ecr.ap-northeast-1.amazonaws.com/dockerbuild :$CIRCLE_SHA1 .
deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
