machine:
  services:
    - docker
general:
  artifacts:
    - "app"
dependencies:
  post:
    - docker build -t $DEV_AWS_ECR_URI:$CIRCLE_SHA1 .
deployment:
  prod:
    branch: master
    commands:
      - bash ./ecs-deploy -k $AWS_ACCESS_KEY_ID -s $AWS_SECRET_ACCESS_KEY -r $AWS_DEFAULT_REGION -c -n "TestDocker" -i ignore
test:
  pre:
    - aws configure list
  override:
    - echo "done "
