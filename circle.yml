machine:
  services:
    - docker
general:
  artifacts:
    - "app"
dependencies:
  post:
    - docker build -t $DEV_AWS_ECR_URI:$CIRCLE_SHA1 .
deployment:
  prod:
    branch: master
    commands:
      - ls -lsa
test:
  pre:
    - mkdir app
  override:
    - echo "done"
